import colors from './colors'
import fontSizes from './fontSizes'
import letterSpaceings from './letterSpacings'
import lineHeights from './lineHeights'
import space from './space'

export const theme = {
  space,
  fontSizes,
  letterSpaceings,
  lineHeights,
  colors,
} as const
